// 이 스크립트는 UniDebug 에디터 윈도우에서 자동 생성됩니다.
// 직접 수정하지 마세요!
// 태그를 변경하려면 UniDebug/Debug Window에서 'Tags...' 버튼을 클릭하세요.

namespace UniDebug
{
    /// <summary>
    /// 디버그 로그에 사용할 수 있는 태그 enum
    /// </summary>
    public enum DebugTag
    {
        Default = 1,
        NewTag1 = 2,
        NewTag2 = 3,
        hehe = 4,
        Test = 5,
    }
}
